// Fixture stub of the vendored Alpine runtime. The real one (~55KB, sha256-pinned by
// tools/vendor-alpine.mjs) lives in every scaffolded project at assets/js/vendor/alpine.min.js;
// the fixture only needs the file to exist so site-graph's emit copies it into the artifact's
// js/vendor/ during --self-test. Nothing here ever executes.
window.Alpine = { data() {}, store() {}, start() {} };
